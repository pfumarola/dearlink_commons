export enum userRoles {
  ADMIN = 'admin',
  USER = 'user',
  VISITOR = 'visitor',
}

export enum bookingStatuses {
  active = 'active',
  canceled = 'canceled',
  completed = 'completed',
  pending = 'pending',
  confirmed = 'confirmed',
}
